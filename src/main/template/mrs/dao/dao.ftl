package ${lowerCase(package)}.${camelCase(module)}.dao;

import java.util.*;
import com.buc.base.dao.BaseDao;
import ${lowerCase(package)}.${camelCase(module)}.entity.${camelFirstUpper(table.tableName)};

public interface ${camelFirstUpper(module)}Dao extends BaseDao<${camelFirstUpper(table.tableName)}>{


}
